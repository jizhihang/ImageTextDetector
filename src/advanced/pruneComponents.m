function [components, bboxes] = pruneComponents(...
                        image, swtImg, components, bboxes, componentModel)
                    
   
                    
                    
    components = components;
    bboxes = bboxes;
end
