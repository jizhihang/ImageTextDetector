function [ drawImg ] = drawRotatedBox(image, box)
    % Function draws bounding box of any orientation
    % Using bresenham and figures out the co-ordinates
    %
    % Usage:
    % drawImg = drawRotatedBox(image, box)
    %
    % Input:
    % image = Image on which the box is to be drawn
    % box = Box of any arbitrary orientation
    %      format : [xTopLeft yTopLeft width height angle]
    %
    % Output:
    % drawImg = image with bounding box drawn
    % 
    
    %Get the rotated box from cannonical form
    
    
    
end

