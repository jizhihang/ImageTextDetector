function[] = computeColorSelfSimilarity()
    % Function to compute the self similarity based on local descriptor
    %
    % Usage :
    % selfSimImg = computeColorSelfSimilarity(image, point)
    %
    % Input:
    % image = Color image for which color self similarity is to be computed
    % 


end
